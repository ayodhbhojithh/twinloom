/**
 * `comfortable` is the blueprint rail: rows sit flush, descriptions on show.
 * `compact` is the lanes rail: every component becomes its own small card and
 * descriptions stay hidden until the rail is widened.
 */
export type ScopeTableDensity = "comfortable" | "compact";
